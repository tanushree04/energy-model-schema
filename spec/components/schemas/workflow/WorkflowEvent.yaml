description: Event
type: object
properties:
  status:
    description: Task status
    type: string
    enum:
      - Submitted
      - Started
      - Succeeded
      - Failed
  timestamp:
    $ref: "#/components/schemas/TimeStamp"
