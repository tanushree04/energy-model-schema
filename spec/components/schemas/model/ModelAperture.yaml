type: object
description: A single planar Static Aperture Face in Model
allOf:
  - $ref: "#/components/schemas/ModelFaceBase"
  - properties:
      type:
        type: string
        enum: ["ModelAperture"]
      face_type:
        type: string
        enum:
          - Window
      is_aperture:
        type: boolean
        enum: [true]
