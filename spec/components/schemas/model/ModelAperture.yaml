type: object
description: A single planar Static Aperture Face in Model
allOf:
  - properties:
      type:
        type: string
        enum:
          - "ModelAperture"
      face_type:
        type: string
        enum:
          - Window
  - $ref: "#/components/schemas/ModelFaceBase"
