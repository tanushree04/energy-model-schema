description: Model event
type: object
required:
  - status
  - timestamp
properties:
  status:
    description: Status of processing a model.
    type: string
    enum:
      - Submitted
      - Created
      - Modified
  timestamp:
    $ref: "#/components/schemas/TimeStamp"
