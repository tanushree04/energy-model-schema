description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        message:
          type: string
          description: A human readble error message.
      example:
        {
          "message": "Success!"
        }
